package com.github.syafiqq.data.datasource.remote.firebase.util

object FirebaseConstants {
    const val REPOSITORY_VERSION = "v1"

    object PATHS {
        object DB {
            const val VERSION = "versions_new"
            const val ORDER = "law_status_order"
        }

        object STORAGE {
            const val UU = "stream"
        }
    }

    const val VERSION_DATE_FORMAT = "yyyy-MM-dd HH:mm:ss"
}
